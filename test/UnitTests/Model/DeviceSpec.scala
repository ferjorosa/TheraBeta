package UnitTests.Model

import Utility.CustomSpec

/**
 * Created by Fer on 03/03/2015.
 */
class DeviceSpec extends CustomSpec{
  "The Device model" should "be able to retrieve a specific device by its identifier" is (pending)
  it should "be able to retrieve all devices belonging to on account" is (pending)
  it should "be able to update a specific device's data"is (pending)
}
